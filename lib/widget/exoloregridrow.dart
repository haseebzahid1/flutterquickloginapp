import 'package:flutter/material.dart';

class ExploreGridRow extends StatelessWidget {
  const ExploreGridRow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
